services:
  spec-mesh:
    image: spec-mesh:latest
    container_name: spec-mesh
    build: 
      context: .
      dockerfile: Dockerfile
    ports:
      - "17045:17045"
    volumes:
      - ./config.yml:/app/config.yml:ro
    restart: unless-stopped